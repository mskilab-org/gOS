import styled from "styled-components";

const Wrapper = styled.div`
  .site-result-error-title {
    font-size: 16px;
    font-weight: bold;
  }
  .site-result-error-icon {
    color: red;
  }
`;

export default Wrapper;
