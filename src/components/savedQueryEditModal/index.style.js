import styled, { createGlobalStyle } from "styled-components";

export const SavedQueryEditModalStyle = createGlobalStyle`
  .saved-query-edit-modal .favorite-query-description {
    margin-bottom: 0;
  }

  .saved-query-edit-modal .favorite-query-description strong {
    color: inherit;
    font-weight: 600;
  }
`;

const Wrapper = styled.div``;

export default Wrapper;